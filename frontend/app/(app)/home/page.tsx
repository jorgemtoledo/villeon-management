// Landing page after login — placeholder until this becomes the real
// dashboard (see NAV_ITEMS comment). Deliberately empty: nothing to show
// yet, this just gives users a neutral first screen instead of dropping
// them straight into Produtos.
export default function HomePage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-xl font-semibold text-foreground">Home</h1>
    </div>
  );
}
