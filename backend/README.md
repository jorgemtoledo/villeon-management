# backend

API Rails do VILLEON Gestão. Instruções de execução (Docker Compose, testes, lint, variáveis de
ambiente) estão no [README da raiz do projeto](../README.md) — o backend não roda sozinho fora do
`docker-compose.yml`, que também sobe Postgres e Redis.
